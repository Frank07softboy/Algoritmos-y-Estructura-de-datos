/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/* 
 * File:   tablaswhileanidado.c
 * Author: Martines II
 *
 * Created on 11 de noviembre de 2020, 12:19 PM
 */

#include <stdio.h>
#include <stdlib.h>

/*
 * 
 */
int main(int argc, char** argv) {

    
    int x  = 1;
    int tope  = 10;
    int tabla = 2;

    while(x<=tope){        
        printf ("%d X %d = %d\n", tabla,x, tabla * x);        
        x++;
    }printf ("%d X %d = %d\n\n", tabla,x, tabla * x);   
    tabla++;
    x=1;   
    
        while (x<10){
            printf ("%d X %d = %d\n", tabla,x, tabla * x);           
            x++;
            
        }printf ("%d X %d = %d\n\n", tabla,x, tabla * x);
        tabla++;
        x=1;
        
            while (x<10){
                printf ("%d X %d = %d\n", tabla,x, tabla * x);               
                x++;
            }printf ("%d X %d = %d\n\n", tabla,x, tabla * x);
            tabla++;
            x=1;
            
                while(x<10){
                    printf ("%d X %d = %d\n", tabla,x, tabla * x);   
                    x++;
                }printf ("%d X %d = %d\n\n", tabla,x, tabla * x);
                tabla++;
                x=1;
                
                    while (x<10){
                        printf ("%d X %d = %d\n", tabla,x, tabla * x);
                        x++;
                    }printf ("%d X %d = %d\n\n", tabla,x, tabla * x);
                    tabla++;
                    x=1;
                    
                        while (x<10){
                            printf("%d X %d = %d\n", tabla, x, tabla * x);
                            x++;
                        }printf ("%d X %d = %d\n\n", tabla,x, tabla * x);
                        tabla++;
                        x=1;
                        
                            while(x<10){
                                printf ("%d X %d = %d\n", tabla,x, tabla * x);
                                x++;                              
                            }printf ("%d X %d = %d\n\n", tabla,x, tabla * x);
                            tabla++;
                            x=1;
                            
                                while(x<10){
                                    printf ("%d X %d = %d\n", tabla,x, tabla * x);
                                    x++;
                                }printf ("%d X %d = %d\n\n", tabla,x, tabla * x);
                                tabla++;
                                x=1;
                                
                                    while (x<10){
                                        printf ("%d X %d = %d\n", tabla,x, tabla * x);
                                        x++;
                                    }printf ("%d X %d = %d\n\n", tabla,x, tabla * x);
                                    tabla++;
                                    x=1;
                                    
                                        while (x<10){
                                            printf ("%d X %d = %d\n", tabla,x, tabla * x);
                                            x++;
                                        }printf ("%d X %d = %d\n\n", tabla,x, tabla * x);

    return (EXIT_SUCCESS);
}

